producer:
kafak的三种发送方式:发送就不管,同步，异步
多线程下producer是线性安全的
保证数据顺序(一个分区)

acks=1(领导副本确认即可)kafka副本机制，肯定都需要复制的，只是没有等从副本复制完成就返回确认消息
acks=0(不用确认)
acks=all  -1全部副本确认



可靠的系統生产者
acks=all或-1
重试机制,确保数据至少写入一次
代码，异常(这些需要重试的消息，写入一个特殊的主题)


kafka的序列化(自定义序列化)
kafka的分区,默认的分区和自定义的分区器



